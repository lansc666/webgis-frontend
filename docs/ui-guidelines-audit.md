# UI 规范复核 · 2026-09-16

依据：[Web Interface Guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md)，本次最终复核前重新读取。
范围：HTML、样式、UI模块、独立体验和两个新增辅助模块。以下为已处理项与明确保留的取舍，不是全量无障碍认证。

## css/style.css

- `css/style.css:32` — 已修复：控件边框对比度2.88:1 → 3.36:1。
- `css/style.css:85` — 已修复：入口说明随内容增高，320px下占位图形不再被挤压。
- `css/style.css:177` — 已修复：长属性局部滚动，不将保存动作推离当前编辑区。
- `css/style.css:218` — 已检查：窄屏快速导航44px目标；目标滚动留白避免吸顶遮挡。
- `css/style.css:248` — 已检查：减少动画偏好。
- `css/style.css:251` — 已检查：强制颜色模式下选中状态仍有轮廓。

## index.html

- `index.html:32` — 已检查：原生导航链接，保持键盘/新标签能力。
- `index.html:124` — 已修复：创建失败关联就近提示，保留输入供重试。
- `index.html:135` — 已修复：属性区有名称、键盘焦点与滚动能力。
- `index.html:137` — 已修复：编辑动作关联错误和操作说明。

## js/ui.js

- `js/ui.js:145` — 已检查：预览文案由明确可选标记控制，正式集成默认不受影响；此标记不是安全边界。
- `js/ui.js:308` — 已修复：草稿弹窗正向/反向Tab循环，Escape取消并返回触发位置。
- `js/ui.js:333` — 已检查：未提交草稿和public临时修改有离开提示；浏览器最终决定是否显示。
- `js/ui.js:380` — 已检查：最多50行DOM；完整数据保留；数字排序、跨页选择和中文组合输入测试通过。
- `js/ui.js:387` — 明确保留：为保留阅读位置，在DOM写入前集中读取一次滚动位置，写入后恢复；未在逐行构建中交错测量。5000要素本机同步测试约7–13ms，若接入超宽真实数据后出现卡顿，再分析并调整。
- `js/ui.js:243` — 明确保留：搜索/分页不写入URL。当前图层和临时数据依赖app.js内存，分享筛选URL无法还原数据，也可能暴露属性关键词。今后有稳定图层链接后，再由app.js统一设计可分享状态。

## 验证边界

自动测试使用本机Edge；外部CDN在回归中被阻断，以验证UI模块可独立运行。真实地图、数据库、Shapefile往返、读屏器和Safari/iOS不在本次验证范围。
已检查语义标签、键盘、尺寸、对比度、减少动画、错误恢复、长内容和多视口；没有宣称所有设备或全部WCAG条目通过。
